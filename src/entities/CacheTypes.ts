enum CacheType {
  ConnectionTypes = "connection_types",
  DataStreamTypes = "datastream_types",
  DataStreamDetails = "datastream_details",
  TargetTypes = "target_types",
  categories = "categories",
  templates = "templates"
}

export default CacheType;
